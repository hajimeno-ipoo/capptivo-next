# はじめの一歩ガイド：Capptivo プロジェクト解説

このドキュメントでは、**Capptivo（カプティーボ）** の全体像と、このフォルダでローカルに修正・開発を進めるための手順を平易な言葉で説明します。

---

## 1. Capptivo とは？

**Capptivo** は、デモ動画・画面録画・スクリーンショットを作成できるオープンソース（MITライセンス）のデスクトップアプリです。
商用の「Screen Studio」などのような高度なズーム演出やマウスカーソル追従・エフェクト、テロップ（自動文字起こし）などを、直感的な操作で手軽に作成できます。

### 主な特徴
- **スマートズーム**: マウスの動きやクリックに合わせて自動で拡大・追従。
- **カーソルエフェクト**: カーソルスタイルの変更、クリック時のアニメーション、軌跡のぼかしなど。
- **背景・アピアランス設定**: 背景グラデーション、角丸、シャドウ、パディングの調整。
- **フェイスカメラ（Webカメラ）合成**: 画面の隅に丸型・四角形のワイプを設置。
- **ローカル完結のAI文字起こし**: whisper.cpp を使用したオンデバイス音声認識による字幕自動生成。
- **タイムライン編集 & 書き出し**: MP4, WebM, GIF への高品質エクスポート。
- **テキストクリップ**: 区間ごとに文字を置き、フォント・色・サイズ・位置を調整。
- **標準的なクリップ編集**: 再生ヘッド位置での分割、不要クリップの削除、クリップ端のトリム。
- **スクリーンショット編集**: macOSで画面・ウィンドウ・範囲を撮影し、動画と同じ編集画面と時間軸で見た目や注釈を編集してPNG・JPEGに書き出します。

---

## 2. システム構成・アーキテクチャ

このアプリは **Tauri（タウリ）** と **React（リアクト）** を組み合わせて作られています。

```
Capptivo
├── フロントエンド (UI・編集画面・タイムライン)
│   └── React 19 + TypeScript + Vite + Tailwind CSS + Pixi.js
│       (録画と画像の画面、操作メニュー、共通の画像合成を担当)
│
└── バックエンド (OS連携・録画・エンコード)
    └── Rust (Tauri 2)
        (画面撮影、高速なマウス座標追跡、FFmpeg連携、動画と画像の保存を担当)
```

- **フロントエンド（UI担当）**: `src/` 配下にあります。画面の見た目やボタン、エディタ操作などを変更したい場合はここを編集します。
- **バックエンド（録画・ネイティブ処理）**: `src-tauri/` 配下にあります。キャプチャ性能やOS固有の処理、動画の保存処理などを変更したい場合はここを編集します。

---

## 3. フォルダ構成の概要

```
capptivo_next/
├── src/                  # React フロントエンドのソースコード
│   ├── components/       # UIコンポーネント (ボタンやダイアログなど)
│   ├── editor/           # 動画編集・プレビュー画面のコアロジック
│   ├── recorder/         # 録画バーやコントロール画面
│   └── ...
├── src-tauri/            # Rust バックエンド (Tauri)
│   ├── src/
│   │   ├── recorder/     # キャプチャやエンコーダ処理
│   │   ├── cursor/       # 60fpsのマウストラッキング処理
│   │   └── commands/     # フロントエンドとRustをつなぐAPI
│   └── Cargo.toml        # Rustの依存関係管理ファイル
├── package.json          # Node.jsパッケージ定義 (pnpm推奨)
└── vite.config.ts        # Vite (ビルドツール) 設定
```

---

## 4. ローカルで修正・実行するための準備

修正作業および動作確認を行うには、以下の環境が必要です。

### 必要なツール
1. **Node.js (v18以上、v22推奨)**: フロントエンド実行用（導入済み確認済み）。
2. **pnpm**: パッケージマネージャー（`npm i -g pnpm` または corepack でインストール）。
3. **Rust (Cargo)**: バックエンドのビルド用。
   - インストールされていない場合は、ターミナルで以下を実行してインストールします：
     ```bash
     curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
     ```

### セットアップ手順

1. **パッケージのインストール**
   ```bash
   pnpm install
   ```

2. **依存バイナリの取得 (FFmpeg等)**
   ```bash
   pnpm fetch:ffmpeg
   ```

3. **ローカル開発サーバーの起動**
   ```bash
   pnpm tauri dev
   ```
   ※ これにより、RustのビルドとViteの開発サーバーが同時に立ち上がり、編集可能なアプリウィンドウが表示されます。コードを変更するとホットリロードですぐに反映されます。

---

## 5. まずどこを触ればいいか？

- **UIや文言、スタイルを修正したい場合**:
  - `src/` 配下のコンポーネントを編集します。
- **録画時の挙動や設定項目を増やしたい場合**:
  - `src-tauri/` 側のコマンド追加と、`src/` 側のIPC呼び出しを接続します。
- **型チェックやリンターを実行したい場合**:
  ```bash
  pnpm lint
  pnpm typecheck
  ```

## 6. タイムライン上の3Dパース

3Dは動画全体の外観設定ではなく、タイムラインに追加する区間ごとのエフェクトです。

- タイムラインの「追加」メニューから「3Dパース」を選ぶと、再生ヘッド付近に3D断片が追加されます。
- 断片をクリックするとLookパネルに設定が開き、プリセットまたはX軸・Y軸・Z軸・パース距離をその断片だけに設定できます。
- 断片の左右端をドラッグして適用区間を変えられます。区間の外では3D値を0として平面表示に戻ります。
- 3D断片は `perspectiveFragments` としてプロジェクトに保存され、プレビューとMP4/WebM/GIF書き出しが同じ再生時刻の断片を参照します。
- Pixi.jsでは録画用の `camera` 部分だけを一時テクスチャへ描画し、`PerspectiveMesh` で四隅を変形します。背景、フェイスカム、字幕はその外側にあるため平面のままです。
- Lookパネルの3Dスライダーやプリセットを変更すると、停止中でも次のプレビュー描画フレームへ即時反映されます。
- 画像や4K動画を書き出す時は、3D変形用の中間画像も最終出力と同じ解像度で作り、1920基準の画像を拡大してぼやけることを防ぎます。
- 3D区間を選ぶと、6つの傾きボタン、X・Y・Zの数値欄、周囲8点と中央の支点を示す図解が使えます。図解の点をクリックすると、その位置がカードを傾ける支点として保存され、角度は変わりません。支点と線で結んだ白い操作点のドラッグでX/Y、支点の周りの青い円弧のドラッグでZを調整します。中央支点でも白い操作点は重ならず、どの支点でも三軸を操作できます。古い3D断片は中央支点として読み込みます。プリセットを選んでも現在の支点を維持します。パース距離スライダーは既存のままです。
- 光の反射は強さ0～100と3種類の見え方を区間ごとに保存します。録画面の上に光を重ね、傾きと同じ描画経路を通すため、編集中の画面と書き出しの両方に反映されます。以前の保存データは反射0で読み込みます。
- 既存の保存データに3D断片がなければ断片は追加されず、従来どおり平面表示になります。保存値は読み込み時に安全な範囲へ収めます。

## 7. 標準的なクリップ編集

タイムラインの一番上のレーンは、録画を再生する「主映像クリップ」のレーンです。

- クリップをクリックして選択し、左右端をドラッグすると、そのクリップの開始・終了をトリムできます。
- 再生ヘッドを切りたい位置に置き、ツールバーの分割ツールまたは `C` キーを使うと、クリップを2つに分割できます。
- 分割後に不要なクリップを選び、Deleteキーまたは削除ボタンを押すと、その区間だけが出力から外れます。
- 切り取られた場所は主映像レーンの空白として表示され、ズームと3Dパースは下の専用レーンで独立して編集できます。
- タイムラインの主映像レーンは常に表示し、ズーム・3Dパース・マスク・ハイライト・速度区間・テキストのレーンは、その種類の内容を追加した時だけ表示します。最後の一つを削除するとレーンも消え、下のレーンが詰まります。追加メニューから再び追加できます。

## 8. macOSの画面収録許可

- ScreenCaptureKitを使うため、`src-tauri/Info.plist` に画面収録とシステム音声の使用目的を記載します。
- macOSの設定でCapptivoを許可した後は、アプリを終了して再起動します。許可状態は起動中のプロセスへ即時反映されないことがあります。
- 開発用のアドホック署名（`signingIdentity: "-"`）は、ビルドごとにmacOSから別のアプリとして扱われることがあります。ビルドを繰り返す場合は、同じビルドを`/Applications/Capptivo.app`から起動して許可を再登録します。
- 配布用に許可を維持するには、Apple DevelopmentまたはDeveloper IDの安定した署名証明書を使います。

## 9. マスク・ハイライト・速度・クリック音

- Lookパネルまたはタイムラインの追加メニューから、「マスク」または「ハイライト」を追加できます。タイムラインではマスクとハイライトを別々のレーンに表示するため、同じ場所に重なって見えません。
- プレビュー上の枠を移動・リサイズしても、選択した種類と開始・終了時刻は保持されます。開始・終了スライダーまたはタイムラインの端を調整すると、必要な場面だけに表示できます。
- 速度は「動画全体」で全体倍率を変え、「速度区間を追加」で一部分だけを0.25〜4倍にできます。「タイピングを自動で2倍速」は、録画に残ったテキストカーソルの区間を入力中の候補として2倍にします。録画時にキーボード入力自体を保存していないため、これは候補検出であり、キー単位の完全な判定ではありません。
- Cursorパネルの「クリック音」をオンにすると、録画済みクリック位置に短い内蔵音を重ねます。音量は0〜100%で、外部の音声ファイルは使いません。
- これらの値は`editorState`へ保存され、プレビューと動画書き出しで同じ時刻を使います。速度区間は音声側でも`atempo`で分割・連結し、クリック音は書き出し後の音声タイムラインへ合成します。
- 速度付き動画のフレーム数は、個々の速度区間ではなく、全区間をつないだ書き出し時間から決めます。短い区間ごとに最低1枚を足すと映像だけが長くなり、音声とずれます。
- マスク・ハイライト・テキストの左側インスペクタープレビューは、`InspectorCompositionFrame`で右側プレイヤーと同じステージ寸法、録画領域、画面切り抜きを使います。元動画をそのまま置いて割合だけを重ねる実装に戻さないでください。
- マスク・ハイライトの選択ハンドルは、見た目だけを上に重ねます。ハンドルの透明な親要素がマウス操作を受け取ると、下のプレビューにドラッグが届かず、移動もサイズ変更もできなくなります。親要素には `pointer-events-none` を指定します。

## 10. ハイライトの色とテキストクリップ

- ハイライトを選ぶと、色と透明度を設定できます。透明度は0%なら見えず、100%なら設定した色がそのまま表示されます。
- ハイライト色を編集画面の半透明な背景へ変換するときは、赤・緑・青をすべて16進数で読み取ります。1色でも通常の10進数として読むと、指定した色が表示されません。

- 「テキストを追加」で、再生ヘッド付近に独立したテキストクリップを作成します。テキストは自動字幕とは別のタイムラインレーンに保存されます。
- 選択中のテキストでは、文字、フォント、文字色、文字サイズ、横位置、縦位置を変更できます。位置は動画全体に対する割合で指定し、プレビューと書き出しで同じ値を使います。
- 位置はスライダーだけでなく、ハイライトと同じ編集プレビュー上のテキスト枠を直接ドラッグして調整できます。スライダーは細かな調整用に残しています。
- フォント候補は固定リストではありません。macOSのCore Textから、そのMacで現在表示可能なフォントファミリーを取得して選びます。取得できない開発用ブラウザでは、保存済みのOS標準フォントを維持します。
- テキストクリップの左右端をドラッグすると表示区間を変えられ、中央をドラッグすると区間全体を移動できます。分割ツールで1つのテキストを2区間に分けることもできます。

## 11. 公開リポジトリへ載せるもの

- アプリのコード、必要な設定、依存関係を固定するファイル、READMEで参照する素材、元コードのMITライセンスと第三者表記を残します。
- 録画や書き出しの成果物、ビルド生成物、端末固有の設定、認証情報や秘密鍵は載せません。元の寄付先設定も、新しいリポジトリには引き継ぎません。
- 新しい公開先から作ったアプリは、新しい公開先の更新情報を確認します。独自の更新用公開鍵は設定済みですが、秘密鍵を安全に保管し、リリース用の署名設定とリリースを用意するまでは自動更新の動作は確認できません。
- 手元用 `.app` は `pnpm tauri build --bundles app` で `src-tauri/target/release/bundle/macos/Capptivo.app` に作ります。自動更新用の秘密鍵がない場合、`.app` の生成後にコマンドが失敗することがあるため、生成物の署名を別に確認します。DMGは生成された `.app` だけを入れた一時フォルダと Tauri の `bundle_dmg.sh` を使い、`src-tauri/target/release/bundle/dmg/Capptivo_1.0.3_aarch64.dmg` に作ります。`bundle/macos` 全体をDMGの入力にすると、自動更新用ファイルまで混入するため使いません。`/Applications` に置くのは利用する `Capptivo.app` １件だけです。旧版のバックアップや退避コピーは作らず、ビルド元・DMG内・インストール先の内容を照合します。詳細な作業上の禁止事項はルートの `AGENTS.md` に記載しています。

## 12. スクリーンショットの仕組みと使い方

- 録画バーの動画・画像アイコンで「録画／スクショ」を切り替えます。ほかの操作と同じ大きさのボタンで、選択中のアイコンに色が付きます。「スクショ」では画面・ウィンドウ・範囲を選び、右端の「スクショ」を押します。動画アイコンに戻すと従来の録画操作になります。macOS 14以降はScreenCaptureKitの静止画API、macOS 13はQuartzの取得経路を使います。画面収録の許可が必要です。
- 録画・スクショのどちらでも、ディスプレイとウィンドウの候補メニューは開いた時と「更新」を押した時に静止サムネイルを取得します。候補を開いている間のライブ更新は行いません。候補を選ぶだけでは録画を始めません。macOSの候補画像は、以前のQuartzによる静止画像取得を使います。候補画像に `SCScreenshotManager` は使いません。通常のスクリーンショット保存は別の処理です。
- ウィンドウ候補はScreenCaptureKitの `onScreenWindowsOnly` で取得し、所有元がOS上で補助アプリや背景専用アプリと分かる窓を除外します。分類を取得できない窓は従来の判定を保ちます。タイトルのない通常ウィンドウも候補に残し、表示名には所有アプリ名を使います。別Space、最小化、非表示のウィンドウを大きさで例外扱いして候補へ戻してはいけません。
- ウィンドウ候補はQuartzの従来の静止画像取得で枠の影を除きます。候補を選ぶと録画対象に設定し、macOSのアクセシビリティ許可がある場合だけ、所有アプリのウィンドウ一覧から選択IDと完全一致する一枚を手前に出します。位置・大きさ・タイトルでは判定しません。IDの取得にはmacOSの非公開APIを使います。前面化APIがエラーを返しても、選択IDの窓と所有アプリが実際に前面なら成功と扱います。許可がない場合、IDを一致させられない場合、選択IDの窓が前面にならない場合は、別の窓を成功扱いせずエラーを表示します。候補選択だけでは録画しません。
- 画面・範囲を撮る間だけ録画バー、範囲枠、描画ツールバーを隠します。表示中のカメラ小窓と描画は画像に写ります。ウィンドウ撮影では `window_prepare` で対象を前面へ出した後、そのウィンドウだけを撮るため、カメラ小窓と描画は写りません。新しいmacOSでは静止画専用の `SCScreenshotConfiguration` と `ignoreClipping`・`ignoreShadows` を使い、画面端の外にある部分や影の透明領域を画像へ混ぜません。利用できないmacOSでは従来の `SCStreamConfiguration`、macOS 13ではQuartzへ戻ります。失敗時も隠した表示を戻します。
- `src-tauri/src/project/store.rs` は元画像 `original.png` と、編集設定を入れる `screenshot.json` を別々に保存します。動画の `project.json` は変えません。画像を編集しても元画像は上書きされません。
- `EditorApp.tsx` が動画・画像に共通の画面です。ライブラリで動画または画像を選ぶと、種類に関係なく現在の編集ウィンドウへ読み直すため、別の編集ウィンドウを増やしません。画像は `screenshot.json` と `original.png` を保ちます。`ProjectStore::load` は画像を動画編集画面に渡すための仮想的な動画プロジェクトを返し、時間軸の時計用に5秒の `screen.mp4` を必要時に生成します。この動画は元画像やPNG・JPEGの描画には使いません。
- `ScreenshotToolsPanel.tsx` は左端の画像タブにあるペン・自由描画のマーカー・矢印・図形・塗りつぶし専用です。文字・範囲ハイライト・ぼかしマスクは共通側だけで編集します。ペン・マーカー・矢印・四角・丸は、配置後に選択すると現在の線の太さがスライダーへ表示され、そこから変更できます。`screenshotModel.ts` は描画を元画像の座標で保存します。`PreviewStage.tsx` は元PNGと描画をキャンバスに描き、動画にも使うPixi合成処理へ渡します。背景・余白・角丸・影・切り抜き・ズーム・3D・ぼかし・ハイライト・文字は共通の編集機能です。新しく撮った画像も動画と同じ背景画像、16:9、余白・角丸・影を初期値にし、利用者が保存済みの「背景なし」は勝手に変更しません。高解像度の画像は、1920基準の編集座標を保ったまま元画像と同じ大きさのGPU出力へ直接描きます。3Dを使う場合も映像部分の中間画像を最終出力と同じ解像度で作るため、4K・5K画像を1920へ縮小してから引き伸ばしません。時間軸は共通画面の構造として表示しますが、画像の編集内容は全期間へ常時適用し、再生位置で表示を変えません。ズームと3Dは画像全体に一つ、マスク・ハイライト・文字は複数を同時に置けます。画像では時間区間の移動・伸縮・分割、速度、カーソル、カメラ、字幕を使用不可にし、動画では画像専用タブを灰色にします。設定は自動保存します。
- 書き出し画面でPNG・JPEGを先に選び、保存ダイアログでは選んだ形式だけを指定します。再生位置に依存しない静止画用の描画結果をプレビューと書き出しで共用し、JPEGの透明部分は黒で埋めます。撮影時に写ったカメラや描画は元PNGの一部なので、後から個別には取り除けません。黒い画面で止まる場合は、時計用MP4の生成、元PNGの読み込み、Pixi合成の順に確認します。
