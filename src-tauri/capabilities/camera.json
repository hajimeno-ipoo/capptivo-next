{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "camera",
  "description": "Camera bubble: write camera WebM chunks only. No recording control or project delete.",
  "windows": ["camera"],
  "permissions": [
    "core:window:allow-start-dragging",
    "allow-mark-camera-started",
    "allow-begin-camera-file",
    "allow-write-camera-chunk",
    "allow-finish-camera-file"
  ]
}
